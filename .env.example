# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# This is your database connection string that Drizzle ORM uses to connect to your database.
# The format varies depending on which database system you're using:
#
# For PostgreSQL (recommended for production):
# DATABASE_URL="postgresql://username:password@localhost:5432/uiux_mockup"
#
# For MySQL:
# DATABASE_URL="mysql://username:password@localhost:3306/uiux_mockup"
#
# For SQLite (good for local development):
# DATABASE_URL="file:./dev.db"
#
# For cloud databases like Neon, Supabase, or PlanetScale, paste the connection
# string they provide in their dashboard.
DATABASE_URL=

# =============================================================================
# CLERK AUTHENTICATION
# =============================================================================
# Clerk is the authentication service used in this application for user management,
# sign-in, and sign-up functionality. You'll need to create a free account at
# https://clerk.com to get these keys.

# Your Clerk Publishable Key - This is safe to expose in client-side code
# Find this in your Clerk Dashboard under API Keys
# It starts with "pk_test_" for development or "pk_live_" for production
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=

# Your Clerk Secret Key - Keep this private and never expose it in client-side code
# Find this in your Clerk Dashboard under API Keys
# It starts with "sk_test_" for development or "sk_live_" for production
CLERK_SECRET_KEY=

# =============================================================================
# CLERK ROUTING CONFIGURATION
# =============================================================================
# These settings control where users are directed during the authentication flow.
# The paths are relative to your application's base URL.

# The URL path where your sign-in page is located
# Users will be redirected here when they need to authenticate
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in

# Where users go after successfully signing in
# The "/" means they'll be redirected to your homepage
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/

# The URL path where your sign-up page is located
# New users will be directed here to create an account
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# Where users go after successfully creating a new account
# The "/" means they'll be redirected to your homepage
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/

# =============================================================================
# OPENROUTER AI API
# =============================================================================
# OpenRouter provides unified access to multiple AI models through a single API.
# This key allows your application to make requests to various AI models like
# GPT-4, Claude, Llama, and others for features like content generation,
# chat functionality, or design assistance.
#
# To get your API key:
# 1. Visit https://openrouter.ai
# 2. Create an account or sign in
# 3. Navigate to your API Keys section
# 4. Generate a new API key
#
# Keep this key secure and never commit it to version control or expose it
# in client-side code.
OPENROUTER_API_KEY=

# =============================================================================
# OPTIONAL: ADDITIONAL CONFIGURATION
# =============================================================================
# Uncomment and configure these if you need them for your specific deployment

# The base URL of your application - useful for generating absolute URLs
# NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Node environment setting - usually set automatically by your hosting provider
# NODE_ENV="development"
